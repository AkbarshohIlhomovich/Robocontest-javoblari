#include <bits/stdc++.h>

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(0);
    long long n, m;
    std::cin >> n >> m;
    long long result = (n * m) / 2;
    std::cout << result << std::endl;
    return 0;
}
