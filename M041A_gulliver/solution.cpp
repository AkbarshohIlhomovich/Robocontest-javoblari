#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    long long k, m;
    cin >> k >> m;

    long long result = m * k * k;

    cout << result << endl;

    return 0;
}
